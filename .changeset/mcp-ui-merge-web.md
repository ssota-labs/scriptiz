---
"@scriptiz/mcp-ui": patch
---

Merge the former `apps/mcp-ui-web` Vite app into `packages/mcp-ui/web`. Use `pnpm dev:web` / `pnpm build:web`; set `SCRIPTIZ_MCP_UI_DIST` to `packages/mcp-ui/web/dist` for MCP Apps HTML.
